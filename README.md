# PINNs_Linear_Elastic
This repository implements Physics-Informed Neural Networks (PINNs) to solve and verify linear elasticity problems. The approach is evaluated using three benchmark cases: an analytical solution, a 2D simulation, and a 3D simulation.
